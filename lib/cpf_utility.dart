/// Generate, validate and format Brazilian CPF's.
///
/// cpf_utility is a simple library where you can:
/// - Generate a CPF from a Brazilian state or not (in this case, the state will be random).
/// - Validate a formatted or unformatted CPF.
/// - Format or strip the format of a CPF.
library cpf_utility;

export 'src/states/states.dart';
export 'src/cpf/cpf.dart';
